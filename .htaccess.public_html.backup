# Backup do .htaccess do public_html (/home/easybudget1/public_html/)
# Data: 2026-02-02

RewriteEngine On
RewriteBase /

# Se o arquivo ou diretório não existe, redirecione para index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
