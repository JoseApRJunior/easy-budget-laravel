<?php

/**
 * Exceção lançada quando uma entidade não é encontrada no banco de dados.
 *
 * Esta classe é uma extensão da classe `Entity` e é utilizada para representar
 * uma exceção que ocorre quando uma entidade não é encontrada no banco de dados.
 *
 * @package core\dbal
 */

namespace core\dbal;

class EntityNotFound extends Entity
{
    /**
     * Construtor da classe.
     *
     * Não há implementação específica para este construtor, pois a classe
     * `EntityNotFound` é apenas uma extensão da classe `Entity`.
     */
}
