import { SPMaskBehavior as e, spOptions as t } from "./masks.min.js";
import {
   initializePasswordValidation as a,
   scrollToElement as i,
   handleFormSubmit as l,
   initTheme as n,
   updatePlanSelection as o,
   togglePassword as r,
} from "./utils.min.js";
function initializeElements() {
   let e = document.querySelectorAll(".select-plan"),
      t = document.getElementById("planSelect"),
      l = document.getElementById("conhecaPlanos");
   l.addEventListener("click", (e) => {
      e.preventDefault(), i("plans");
   }),
      e.forEach((l) => {
         l.addEventListener("click", () => {
            let n = l.getAttribute("data-plan");
            o(n, e, t), i("preCadastroForm");
         });
      }),
      document.querySelectorAll(".password-toggle").forEach((e) => {
         e.addEventListener("click", () => {
            r(e.getAttribute("data-input"));
         });
      });
}
function initializeFormBehavior() {
   $("#phone").mask(e, t);
   let n = document.querySelector("form");
   n.addEventListener("submit", l);
}
function selectPlan() {
   let e = document.querySelectorAll(".select-plan"),
      t = document.getElementById("planSelect");
   e.forEach((l) => {
      l.addEventListener("click", () => {
         let n = l.getAttribute("data-plan");
         o(n, e, t);
         let a = document.getElementById("preCadastroForm");
         a && a.scrollIntoView({ behavior: "smooth" });
      });
   });
}
document.addEventListener("DOMContentLoaded", function () {
   initializeElements(), initializeFormBehavior(), a(), n(), selectPlan();
});
