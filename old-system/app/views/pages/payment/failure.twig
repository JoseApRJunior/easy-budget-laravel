{% extends "layout.twig" %}



{% block content %}
<div class="container mt-5">
	<div class="row justify-content-center">
		<div class="col-md-8">
			<div class="card">
				<div class="card-body text-center">
					<h2 class="card-title text-danger">
						<i class="bi bi-exclamation-triangle"></i>
						Falha no Pagamento
					</h2>
					<p class="card-text">
						{{ message }}
					</p>
					<hr>
					<h4>Detalhes da Transação</h4>
					<ul class="list-unstyled">
						<li>
							<strong>ID da Transação:</strong>
							{{ payment_id }}
						</li>

						<li>
							<strong>Plano:</strong>
							{{ plan_name }}
						</li>
						<li>
							<strong>Valor:</strong>
							R$
							{{ plan_price|number_format(2, ',', '.') }}
						</li>
						<li>
							<strong>Data:</strong>
							{{ transaction_date|date("d/m/Y H:i:s") }}
						</li>
					</ul>

					<hr>
					<p>
						Se o problema persistir, entre em contato com nosso suporte.
					</p>
					<a href="/plans" class="btn btn-primary">Tentar Novamente</a>
					<a href="/provider" class="btn btn-secondary">Voltar para o Dashboard</a>
				</div>
			</div>
		</div>
	</div>
</div>
{% endblock %}
